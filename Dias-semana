#include <stdio.h>
#include <stdlib.h>
#include <time.h>

enum Dias{LUNES = 0, MARTES = 1 , MIERCOLES = 2 , JUEVES = 3 , VIERNES = 4 , SABADO = 5 , DOMINGO = 6};

int main()
{

int numero;

printf("Ingrese el Numero u Ordinal: \n");
scanf("%d", &numero);
printf("-------------------------\n");

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

enum Dias{LUNES = 0, MARTES = 1 , MIERCOLES = 2 , JUEVES = 3 , VIERNES = 4 , SABADO = 5 , DOMINGO = 6};

int main()
{

int numero;

printf("Ingrese el Numero u Ordinal: \n");
scanf("%d", &numero);
printf("-------------------------\n");
if (numero == 0)
{
    printf("El Dia es Lunes");
}
else
{
    if(numero == 1 )
    {
        printf("El Dia es Martes");
    }
    else
    {
        if (numero == 2)
        {
           printf("El Dia es Miercoles");
        }
        else
        {
            if (numero == 3)
            {
               printf("El Dia es Jueves");
            }
            else
            {
                if (numero == 4)
                {
                    printf("El Dia es Viernes");
                }
                else
                {
                    if (numero == 5)
                    {
                        printf("El Dia es Sabado");
                    }
                    else
                    {
                        if (numero == 6)
                        {
                            printf("El Dia es Domingo");
                        }
                        else
                        {
                            if (numero > 6)
                            {
                                printf("El Dia no existe");
                            }
                            
                        }
                        
                        
                    }
                    
                }
                
                
            }
            
            
        }
    }   
}

}
}
